# -- Commands

Init new node app
λ npm init -y

Install dependencies
λ npm install http express debug --save
λ npm install body-parser --save
λ npm install
     
Install nodemon on dev mode
λ npm install nodemon --save-dev
